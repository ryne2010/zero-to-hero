# Prompt: canonical cleanup

```txt
Use the zero-to-hero skill.
Run lossless canonical cleanup.

Remove iteration residue, conflicting source-of-truth docs, duplicate ADRs/indexes, broken references, invalid YAML, ambiguous UI source locations, and rejected/sample-only design language.

Preserve all hard truths, final design decisions, product constraints, user stories, workflows, and safety boundaries.

Run cleanup/reference/YAML checks and write .codex/reports/zero-to-hero/cleanup-report.md.
```

Do not implement product code.

