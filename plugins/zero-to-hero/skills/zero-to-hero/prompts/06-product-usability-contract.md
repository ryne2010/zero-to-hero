# Prompt: product usability contract

```txt
Use the zero-to-hero skill.
Generate the Product Usability Contract.

Create docs/product-execution/ with action binding, form lifecycle, dataflow, workflow transaction, scenario seed, route capability, local service, no-dead-ends, and workflow evidence contracts.

Every visible priority control must map to an action or explicit disabled reason. Every priority form must validate, submit, persist, handle errors, and update dependent state.
```

Do not implement product code.

